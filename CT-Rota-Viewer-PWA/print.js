/**
 * print.js
 * --------
 * Placeholder. The dashboard has no print feature yet - printing is
 * handled by the Rota Manager app's print preview. This file stays in
 * place (and in the service worker cache list) so adding a print
 * feature later doesn't require touching the cache configuration.
 */
